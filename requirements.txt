numpy>=1.26
scipy>=1.11
PyYAML>=6.0
matplotlib>=3.8

# Recommended open-source comparison solvers.
osqp>=0.6.3
cvxpy>=1.4
clarabel>=0.9
pyscipopt>=5.0

# Testing.
pytest>=8.0
pytest-cov>=5.0

# Gurobi requires a separate license. Uncomment if it is not already installed.
# gurobipy>=11.0

# The pinned CPU/GPU/QPU stack is defined by the ``full`` project extra.
# Do not list an Aer distribution here: CPU Aer and both GPU Aer packages own
# the same ``qiskit_aer`` import tree.  From the repository root run either:
# python -m pip install -e ".[full]"       # clean environment
# python scripts/install_environment.py    # repair or install, then verify

# Interactive Copilot (optional).
# streamlit>=1.40
# plotly>=5.24
